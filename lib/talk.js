// Generated by CoffeeScript 1.8.0
(function() {
  var Client, Talk, api, client, config, logger, talk, _;

  logger = require('graceful-logger').format('medium');

  _ = require('lodash');

  config = require('./config');

  api = require('./api');

  Client = require('./client');

  client = new Client;

  Talk = (function() {
    function Talk() {}

    Talk.prototype.init = function(_config) {
      if (_config == null) {
        _config = {};
      }
      config = _.extend(config, _config);
      api.fetch();
      return this;
    };

    Talk.prototype.authClient = function(token) {
      return new Client(token);
    };

    Talk.prototype.call = function() {
      return client.call.apply(client, arguments);
    };

    return Talk;

  })();

  talk = new Talk;

  talk.service = require('./service');

  module.exports = talk;

}).call(this);
