// Generated by CoffeeScript 1.7.1
(function() {
  var Client, EventEmitter, Talk, config, server, _,
    __hasProp = {}.hasOwnProperty,
    __extends = function(child, parent) { for (var key in parent) { if (__hasProp.call(parent, key)) child[key] = parent[key]; } function ctor() { this.constructor = child; } ctor.prototype = parent.prototype; child.prototype = new ctor(); child.__super__ = parent.prototype; return child; };

  EventEmitter = require('events').EventEmitter;

  _ = require('lodash');

  config = require('./config');

  server = require('./server');

  Client = require('./client');

  Talk = (function(_super) {
    __extends(Talk, _super);

    function Talk() {
      this.client = new Client;
    }

    Talk.prototype.init = function(_config) {
      if (_config == null) {
        _config = {};
      }
      config = _.extend(config, _config);
      return this;
    };

    Talk.prototype.useServer = function(app, options) {
      if (options == null) {
        options = {};
      }
      return server.initialHandler(app, options);
    };

    Talk.prototype.auth = function(token) {
      var client;
      client = new Client(token);
      return client;
    };

    Talk.prototype.discover = function() {
      return this.client.discover.apply(this.client, arguments);
    };

    Talk.prototype.call = function() {
      return this.client.call.apply(this.client, arguments);
    };

    return Talk;

  })(EventEmitter);

  module.exports = new Talk;

}).call(this);
